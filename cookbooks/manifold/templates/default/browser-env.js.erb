if (!window.process) window.process = {};
if (!window.process.env) window.process.env = {};
process.env.DOMAIN = "<%= @domain %>";
process.env.NODE_ENV = "production";
process.env.SSL_ENABLED = <%= @nginx["listen_https"] ? "true" : "false" %>;
